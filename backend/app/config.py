# from pydantic_settings import BaseSettings


# class CsrfSettings(BaseSettings):
#     secret_key: str = "your-secure-random-secret-key"  # Replace with a strong key
#     cookie_name: str = "csrf_token"  # Name of the cookie used for CSRF tokens
#     header_name: str = "X-CSRF-Token"  # Header to look for CSRF tokens

#     class Config:
#         env_file = ".env"  # Optional: load the key from an environment file
